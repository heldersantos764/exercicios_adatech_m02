// Escreva um programa em JavaScript que verifica se 
// um número é par ou ímpar.
// Use a estrutura if-else para realizar essa verificação.
let number = 2;
const isEvenNumber = number % 2 === 0;

console.log(
    isEvenNumber ? `O número ${number} é par.` : `O número ${number} é ímpar.`
);